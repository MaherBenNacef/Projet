{% extends 'base.html.twig' %}

{% block body %}
    <div align="center">
    <h1>Chambre</h1>
        <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Nb_lit</th>
                <td>{{ chambre.nblit }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ chambre.prix }}</td>
            </tr>
            <tr>
                <th>Numero_chambre</th>
                <td>{{ chambre.numeroChambre }}</td>
            </tr>
        </tbody>
    </table><br><br>


            <a href="{{ path('chambre_index') }}" class="btn btn-info">Back to the list</a><br>

            <a href="{{ path('chambre_edit', { 'id': chambre.id }) }}" class="btn btn-warning">Edit</a><br>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
    </div>
    <br><br>
    {% block style %}
        <style>
            table {
                font-family: arial, sans-serif;
                border-collapse: collapse;
                width: 30%;
            }

            td, th {
                border: 1px solid #dddddd;
                text-align: left;
                padding: 8px;
            }

            tr:nth-child(even) {
                background-color: #dddddd;
            }
            .btn-danger{
                width: 135px;
                height: 40px;
            }
            .btn-warning{
                width: 135px;
                height: 40px;
            }
        </style>
    {% endblock %}
{% endblock %}
